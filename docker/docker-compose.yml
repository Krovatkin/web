version: '3.8'

services:
  localsend-web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8675:443"  # HTTPS for WebRTC discovery
      - "8676:80"   # HTTP for manual peer connections
    restart: unless-stopped
