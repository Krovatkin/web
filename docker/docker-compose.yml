version: '3.8'

services:
  localsend-web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    # Use host networking to receive UDP multicast for native client discovery
    network_mode: host
    restart: unless-stopped
